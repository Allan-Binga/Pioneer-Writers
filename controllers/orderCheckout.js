const client = require("../config/dbConfig");
const dotenv = require("dotenv");
dotenv.config();

//Create Paypal Checkout
const paypalCheckout = (req, res) => {
  try {
  } catch (error) {}
};

module.exports = {
  paypalCheckout,
};
